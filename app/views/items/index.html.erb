<%= render partial: 'share/group_members_sidebar' %>

<div class="col-lg-9">
    <%= modal_button('Add Item', new_group_list_item_path(@group, @list))%>
    <%= render partial: 'item', collection: @list.items %>
</div>
